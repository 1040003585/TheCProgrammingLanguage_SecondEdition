#include <stdio.h>

#define LOWER 0
#define UPPER 300;
#define STEP  20

int main()
{
	int fahr;int STEPPED;//

	for(fahr = LOWER; fahr <= UPPER fahr = fahr + STEP)
		printf("STEP// %#X,0x%06x\n", fahr, (5.0/9.0)*(fahr-32));
}
